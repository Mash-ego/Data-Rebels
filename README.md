# Data-Rebels
Standard bank hackathon
for reproducibility - please have the files (csv's afs csv and fx_rates) in one folder (directory) with the given data set (the 3 csv's) which i did not upload
start with the pipeline -> pipeline validate script -> diagnostics (optional) -> charts -> app (dashboard)
